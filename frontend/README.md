# Frontend — Task Manager

Ver la documentación completa del proyecto en el [README de la raíz](../README.md).

## Desarrollo local

```bash
npm install
npm run dev
```

Requiere un archivo `.env.local` con `VITE_API_URL` apuntando a la API (ver `../README.md#variables-de-entorno`).
